import { Vessel } from "../../models/cruiseShippingModels/v1/vessel"

// Path localhost:3000/cruiseShips/vessel
export function vessel_get(req, res, next) {
  res.send("NOT IMPLEMENTED: vessel path in the vesselController")

  //  Vessel.find().then(vessels => res.json(vessels))
}
